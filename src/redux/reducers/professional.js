const INITIAL_STATE = {
  resume: '',
  role: '',
  description: '',
};

function professionalData(state = INITIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default professionalData;
